// import 'package:flutter_test/flutter_test.dart';

// import 'package:zwo_achtundvierzig/main.dart';

void main() {
// This one still needs a lot of time, but I will keep it here.
// TODO: Fix the Widget Test `Restart and Confirm button mechanic`

//   testWidgets('Restart and Confirm button mechanic', (WidgetTester tester) async {
//     // Build our app and trigger a frame.
//     await tester.pumpWidget(MyApp());

//     // Verify: Restart button is there, Confirm button isn't.
//     expect(find.text('Restart'), findsOneWidget);
//     expect(find.text('Confirm'), findsNothing);

//     // Hit the Restart button
//     await tester.tap(find.text('Restart'));
//     await tester.pump();

//     var a = find.text('Confirm');

//     // Verify: Confirm button is there
//     // expect(find.text('Con'), findsOneWidget);

//     // Hit the Confirm button
//     await tester.tap(find.text('Confirm'));
//     await tester.pump();

//     // Verify: Confirm button is gone
//     expect(find.text('Confirm'), findsNothing);

//     // Verify: Game is reset, Score is 0 again
//     expect(find.text('Score: 0'), findsOneWidget);
//   });
}
